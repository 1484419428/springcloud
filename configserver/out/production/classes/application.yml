spring:
  cloud:
    config:
      server:
        git:
          uri: https://github.com/forezp/SpringcloudConfig/
          search-paths: respo
        default-label: master
  application:
    name: config-server
server:
  port: 8888